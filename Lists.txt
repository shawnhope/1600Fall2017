Lists
Like arrays, but better, you don't have to know how many elements it will have ahead of time. NEEDs namespace: using System.Collection.Generic

//-------------------------------------------------1
List<int> weapons = new List<int>();

void update()
if (Input.GetKeyDown (KeyCode.Space))
{
	weapons.add();
}
//-------------------------------------------------2